[package]
name = "parallel_port"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/parallel_port.rs"

[dependencies]
serde = { workspace = true, features = ["derive"] }
